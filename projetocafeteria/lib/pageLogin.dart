import 'package:flutter/material.dart';
import 'package:projetocafeteria/bodyLogin.dart';

class PageLogin extends StatelessWidget {
  const PageLogin({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
    body: BodyLogin() ,
  );
  
}